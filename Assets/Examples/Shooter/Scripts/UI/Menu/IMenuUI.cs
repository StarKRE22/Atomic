using Atomic.Entities;

/**
 * Created by Entity Domain Generator.
 */

namespace ShooterGame.UI
{
    /// <summary>
    /// Represents a specialized entity interface that extends the core <see cref="IEntity"/> contract.
    /// It follows the Entity–State–Behaviour architectural pattern, providing structure for identity (tags),
    /// data (values), and modular behaviours within the Atomic Entity framework.
    /// </summary>
    /// <remarks>
    /// Created by <b>Entity Domain Generator</b>.
    /// </remarks>
    public interface IMenuUI : IEntity
    {
    }
}
